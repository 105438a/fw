bytes（每秒的 bytes）
這是「在上一個 1 秒統計區間內，該 UART 埠實際收到的原始位元組數」。包含數字、逗號、負號、空白、以及行尾字元（CR/LF/0x1C）等所有字元。
→ 可以當作資料吞吐量（bytes/s）。

lines（每秒的 lines）
這是「在上一個 1 秒統計區間內，該 UART 埠成功切出『一行』的次數」。
在你的程式裡，一行是指：累積到行尾字元（LF 或 CR，或 0x1C），呼叫 handleLine() 成功結束並送出 TAG,ms,payload 的那一次。
→ 可以當作每秒成功解析出幾筆資料（筆數/s ≈ 取樣率）。
